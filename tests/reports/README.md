# Test Reports

## Schema
- `schemas/` - JSON schemas for result validation
- `integration-requests/` - TCR (Test Change Request) from subagents to parent agent

## Output
All result files are written to `test-results/` by `scripts/test/write-test-result.ts`.
