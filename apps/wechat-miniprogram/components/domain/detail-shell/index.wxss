@import "../../../styles/index.wxss";

.detail-shell {
  display: block;
}

.detail-shell__topbar {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin: 0 -32rpx 28rpx;
  padding: calc(env(safe-area-inset-top) + 20rpx) 252rpx 20rpx 32rpx;
  background: rgba(251, 251, 248, 0.75);
  -webkit-backdrop-filter: blur(60rpx) saturate(1.5);
  backdrop-filter: blur(60rpx) saturate(1.5);
}

.detail-shell__back {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 80rpx;
  height: 80rpx;
  flex: 0 0 80rpx;
  border-radius: 50%;
  color: var(--color-ink);
  background: rgba(255, 255, 252, 0.9);
  font-size: 48rpx;
  line-height: 1;
  text-decoration: none;
}

.detail-shell__top-title,
.detail-shell__title,
.detail-shell__meta,
.detail-shell__eyebrow {
  overflow: hidden;
  text-overflow: ellipsis;
}

.detail-shell__top-title {
  min-width: 0;
  color: var(--color-ink);
  font-size: 30rpx;
  font-weight: 700;
  line-height: 1.3;
  white-space: nowrap;
}

.detail-shell__hero {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  aspect-ratio: 3 / 4;
  overflow: hidden;
  border-radius: var(--radius-card-inner);
  background: var(--color-mist);
}

.detail-shell__image {
  width: 100%;
  height: 100%;
}

.detail-shell__badge {
  position: absolute;
  top: 24rpx;
  left: 24rpx;
  z-index: 1;
  padding: 8rpx 16rpx;
  border-radius: var(--radius-control);
  color: var(--color-surface);
  background: rgba(29, 34, 40, 0.58);
  font-size: var(--font-label);
  font-weight: 700;
}

.detail-shell__fallback {
  color: var(--color-muted);
  font-size: var(--font-body);
}

.detail-shell__heading {
  margin-top: 26rpx;
}

.detail-shell__eyebrow {
  color: var(--color-ai);
  font-size: var(--font-caption);
  line-height: 1.4;
}

.detail-shell__title {
  margin-top: 6rpx;
  color: var(--color-ink);
  font-size: 42rpx;
  font-weight: 800;
  line-height: 1.18;
}

.detail-shell__meta {
  margin-top: 8rpx;
  color: var(--color-muted);
  font-size: var(--font-body);
  line-height: 1.45;
}
