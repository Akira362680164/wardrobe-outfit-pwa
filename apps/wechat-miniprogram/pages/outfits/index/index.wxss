.page-shell { padding: 0 32rpx 180rpx; }

.outfit-hero {
  position: relative;
  min-height: 184rpx;
  min-width: 0;
}

.hero-copy { min-width: 0; flex: 1; }

.hero-actions {
  position: absolute;
  right: 0;
  top: 86rpx;
  display: flex;
  justify-content: flex-end;
  gap: 16rpx;
  width: 330rpx;
}

.content-fab::after,
.mini-button::after,
.hero-control::after,
.week-empty__button::after,
.worn-button::after,
.backup-button::after,
.secondary-button::after { border: 0; }

.content-fab {
  position: fixed;
  right: 32rpx;
  bottom: calc(env(safe-area-inset-bottom) + 188rpx);
  z-index: 20;
  width: 112rpx;
  height: 112rpx;
  margin: 0;
  padding: 0;
  border: 0;
  border-radius: var(--radius-fab);
  background: var(--color-primary);
  color: var(--color-surface);
  box-shadow: 0 22rpx 56rpx rgba(53, 92, 125, 0.22);
  font-size: 52rpx;
  font-weight: 700;
  line-height: 104rpx;
  text-align: center;
}

.status-card,
.outfit-card {
  box-sizing: border-box;
  border: 1rpx solid var(--color-line);
  background: rgba(255, 255, 252, 0.78);
  box-shadow: var(--shadow-card);
}

.status-card {
  margin-top: 28rpx;
  padding: 32rpx;
  border-radius: var(--radius-card-inner);
  color: var(--color-muted);
  font-size: var(--font-body);
}

.status-title {
  margin-bottom: 12rpx;
  color: var(--color-ink);
  font-size: var(--font-section);
  font-weight: 700;
}

.status-copy {
  color: var(--color-muted);
  font-size: var(--font-body);
  line-height: 1.55;
}

.mini-button {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 88rpx;
  padding: 0;
  border: 0;
  border-radius: var(--radius-control);
  background: var(--color-primary);
  color: var(--color-surface);
  font-size: var(--font-body);
  font-weight: 700;
  line-height: 1;
}

.hero-control {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  height: 88rpx;
  min-height: 88rpx;
  margin: 0;
  padding: 0;
  border: 0;
  border-radius: 44rpx;
  font-size: 30rpx;
  font-weight: 800;
  line-height: 88rpx;
  white-space: nowrap;
}

.hero-control--calendar {
  width: 148rpx;
  min-width: 148rpx;
  max-width: 148rpx;
  background: rgba(255, 255, 252, 0.92);
  color: #355C7D;
  box-shadow: 0 12rpx 28rpx rgba(29, 34, 40, 0.10);
}

.hero-control--plan {
  width: 164rpx;
  min-width: 164rpx;
  max-width: 164rpx;
  background: #355C7D;
  color: #FFFFFC;
  box-shadow: 0 18rpx 44rpx rgba(53, 92, 125, 0.22);
}

.mini-button {
  width: 320rpx;
  margin: 28rpx 0 0;
}

.week-card {
  margin-top: 26rpx;
  padding: 28rpx 24rpx 24rpx;
  border: 1rpx solid rgba(29, 34, 40, 0.06);
  border-radius: 36rpx;
  background: rgba(255, 255, 252, 0.82);
  box-shadow: 0 24rpx 60rpx rgba(29, 34, 40, 0.08);
}

.week-card__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14rpx;
  min-width: 0;
}

.week-card__copy {
  display: flex;
  align-items: baseline;
  min-width: 0;
  gap: 14rpx;
}

.week-card__title {
  flex: 0 0 auto;
  color: #1D2228;
  font-size: 30rpx;
  font-weight: 800;
}

.week-card__range {
  overflow: hidden;
  color: rgba(29, 34, 40, 0.50);
  font-size: 26rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.week-card__arrows {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex: 0 0 auto;
}

.week-arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
  color: #1D2228;
  font-size: 48rpx;
  font-weight: 700;
  line-height: 1;
}

.week-strip {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 6rpx;
  margin-top: 20rpx;
}

.week-day {
  display: flex;
  min-width: 0;
  height: 148rpx;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  padding-top: 10rpx;
  border: 1rpx solid transparent;
  border-radius: 28rpx;
  color: var(--color-muted);
  box-sizing: border-box;
}

.week-day--active {
  border-color: rgba(53, 92, 125, 0.28);
  background: rgba(53, 92, 125, 0.10);
  color: #355C7D;
}

.week-day__label,
.week-day__plus {
  font-size: var(--font-label);
  line-height: 1.2;
}
.week-day__bars { display: flex; gap: 4rpx; height: 10rpx; margin-top: 12rpx; }
.week-day__bar { width: 30rpx; height: 8rpx; border-radius: 999rpx; }
.week-day__bar.tone-denim { background: rgba(53, 92, 125, 0.42); }.week-day__bar.tone-moss { background: rgba(95, 112, 88, 0.42); }.week-day__bar.tone-clay { background: rgba(185, 113, 85, 0.42); }.week-day__bar.tone-amber { background: rgba(244, 190, 66, 0.72); }.week-day__bar.tone-rose { background: rgba(244, 113, 130, 0.62); }.week-day__bar.tone-purple { background: rgba(188, 140, 236, 0.62); }.week-day__bar.tone-slate { background: rgba(148, 163, 184, 0.62); }

.week-day__date {
  margin-top: 8rpx;
  color: #1D2228;
  font-size: 30rpx;
  font-weight: 800;
  line-height: 1;
}

.week-day__plus {
  margin-top: 12rpx;
  color: rgba(29, 34, 40, 0.30);
}

.week-day__thumb {
  width: 48rpx;
  height: 58rpx;
  margin-top: 10rpx;
  border-radius: 8rpx;
  background: var(--color-mist);
}

.week-day__bar {
  width: 44rpx;
  height: 8rpx;
  margin-top: 8rpx;
  border-radius: 999rpx;
}

.tone-denim { background: #355C7D; }
.tone-moss { background: #5F7058; }
.tone-clay { background: #B97155; }
.tone-amber { background: #F4BE42; }
.tone-rose { background: #F47182; }
.tone-purple { background: #BC8CEC; }
.tone-slate { background: #94A3B8; }

.week-plan-card {
  margin-top: 22rpx;
  padding: 24rpx;
  border: 1rpx solid rgba(29, 34, 40, 0.06);
  border-radius: 32rpx;
  background: rgba(255, 255, 252, 0.72);
}

.week-plan__head {
  display: flex;
  align-items: center;
  gap: 16rpx;
  min-width: 0;
}

.week-plan__date {
  flex: 0 0 auto;
  color: #1D2228;
  font-size: 30rpx;
  font-weight: 800;
}

.week-plan__chip {
  min-width: 0;
  overflow: hidden;
  padding: 8rpx 16rpx;
  border: 1rpx solid rgba(53, 92, 125, 0.20);
  border-radius: 999rpx;
  background: rgba(53, 92, 125, 0.12);
  color: #355C7D;
  font-size: 24rpx;
  font-weight: 800;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.week-plan__body {
  display: flex;
  align-items: center;
  gap: 24rpx;
  margin-top: 22rpx;
}

.week-plan__covers {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  gap: 8rpx;
  min-width: 120rpx;
}

.week-plan__cover {
  width: 120rpx;
  height: 150rpx;
  border-radius: 14rpx;
  background: var(--color-mist);
}

.cover-collage {
  display: grid;
  overflow: hidden;
  background: rgba(244, 245, 243, 0.78);
}

.cover-collage--1 {
  grid-template-columns: 1fr;
}

.cover-collage--2 {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.cover-collage--3 {
  grid-template-columns: repeat(2, minmax(0, 1fr));
  grid-template-rows: repeat(2, minmax(0, 1fr));
}

.cover-collage--3 .cover-collage__cell:first-child {
  grid-column: 1 / 3;
}

.cover-collage--4 {
  grid-template-columns: repeat(2, minmax(0, 1fr));
  grid-template-rows: repeat(2, minmax(0, 1fr));
}

.cover-collage__cell {
  overflow: hidden;
  border-right: 1rpx solid rgba(255, 255, 252, 0.72);
  border-bottom: 1rpx solid rgba(255, 255, 252, 0.72);
}

.cover-collage__image {
  width: 100%;
  height: 100%;
}

.week-plan__cover--empty {
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(29, 34, 40, 0.32);
  font-size: 30rpx;
  font-weight: 800;
}

.week-plan__main {
  min-width: 0;
  flex: 1;
}

.week-plan__name {
  overflow: hidden;
  color: #1D2228;
  font-size: 38rpx;
  font-weight: 800;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.week-plan__actions {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-top: 24rpx;
}

.worn-button,
.backup-button,
.secondary-button {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 64rpx;
  margin: 0;
  padding: 0;
  border-radius: 999rpx;
  font-size: 26rpx;
  font-weight: 800;
  line-height: 64rpx;
}

.worn-button {
  width: 164rpx;
  border: 0;
  background: #355C7D;
  color: #FFFFFC;
}

.worn-button.is-planned {
  background: #355C7D;
}

.worn-button.is-worn {
  border: 1rpx solid rgba(29, 34, 40, 0.10);
  background: #FFFFFC;
  color: rgba(29, 34, 40, 0.56);
}

.worn-button.is-changed {
  background: #355C7D;
}

.backup-button,
.secondary-button {
  width: 220rpx;
  border: 1rpx solid rgba(29, 34, 40, 0.10);
  background: #FFFFFC;
  color: rgba(29, 34, 40, 0.45);
}

.secondary-button {
  width: 164rpx;
  border-color: rgba(53, 92, 125, 0.20);
  background: rgba(53, 92, 125, 0.05);
  color: #355C7D;
}

.backup-list {
  margin-top: 22rpx;
  padding-top: 18rpx;
  border-top: 1rpx solid rgba(29, 34, 40, 0.08);
}

.backup-list__title {
  margin-bottom: 12rpx;
  color: rgba(29, 34, 40, 0.46);
  font-size: 22rpx;
  font-weight: 700;
}

.backup-list__item {
  display: flex;
  align-items: center;
  gap: 14rpx;
  min-width: 0;
  padding: 10rpx 0;
}

.backup-list__cover {
  width: 64rpx;
  height: 64rpx;
  flex: 0 0 64rpx;
  border-radius: 12rpx;
  background: var(--color-mist);
}

.backup-list__cover--empty {
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(29, 34, 40, 0.32);
  font-size: 22rpx;
  font-weight: 800;
}

.backup-list__name {
  min-width: 0;
  overflow: hidden;
  color: var(--color-ink);
  font-size: 24rpx;
  font-weight: 700;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.week-empty {
  margin-top: 22rpx;
  padding: 28rpx;
  border-radius: var(--radius-card-inner);
  background: rgba(244, 245, 243, 0.66);
  text-align: center;
}

.week-empty__title {
  color: var(--color-muted);
  font-size: var(--font-body);
}

.week-empty__copy {
  margin-top: 8rpx;
  color: rgba(29, 34, 40, 0.42);
  font-size: var(--font-caption);
}

.week-empty__button {
  width: 180rpx;
  min-height: 64rpx;
  margin: 18rpx auto 0;
  padding: 0 20rpx;
  font-size: var(--font-caption);
  line-height: 64rpx;
}

.outfit-list {
  display: flex;
  flex-direction: column;
  margin-top: 24rpx;
}

.filter-row {
  display: flex;
  width: calc(100vw - 64rpx);
  white-space: nowrap;
}

.outfit-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 24rpx;
  margin-top: 20rpx;
}

.filter-chip {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 52rpx;
  margin-right: 22rpx;
  padding: 0 22rpx;
  border: 1rpx solid transparent;
  border-radius: var(--radius-control);
  color: var(--color-muted);
  font-size: var(--font-caption);
  font-weight: 700;
}

.filter-chip--active {
  border-color: rgba(53, 92, 125, 0.28);
  background: rgba(53, 92, 125, 0.10);
  color: var(--color-primary);
}

.outfit-card {
  display: block;
  min-width: 0;
  overflow: hidden;
  padding: 0;
  border-radius: var(--radius-card-inner);
}

.outfit-collage {
  height: 246rpx;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-mist);
  background-image: linear-gradient(135deg, rgba(53, 92, 125, 0.14), rgba(255, 255, 252, 0.72));
}

.outfit-collage > .cover-collage {
  width: 100%;
  height: 100%;
}

.collage-empty {
  display: flex;
  width: 100%;
  height: 100%;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  color: rgba(29, 34, 40, 0.48);
  font-size: var(--font-caption);
  font-weight: 800;
}

.collage-image {
  width: 100%;
  height: 100%;
}

.outfit-main {
  min-width: 0;
  padding: 20rpx 20rpx 22rpx;
}

.outfit-name {
  overflow: hidden;
  color: var(--color-ink);
  font-size: 26rpx;
  font-weight: 700;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.outfit-meta {
  overflow: hidden;
  margin-top: 10rpx;
  color: var(--color-muted);
  font-size: 22rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.outfit-wear {
  margin-top: 8rpx;
  color: rgba(29, 34, 40, 0.42);
  font-size: 22rpx;
}

.sheet-options {
  display: flex;
  flex-direction: column;
  gap: 22rpx;
}

.sheet-option {
  padding: 28rpx;
  border: 1rpx solid rgba(29, 34, 40, 0.10);
  border-radius: 24rpx;
  background: rgba(255, 255, 252, 0.90);
}

.sheet-option__title {
  color: #1D2228;
  font-size: 30rpx;
  font-weight: 800;
}

.sheet-option__copy {
  margin-top: 10rpx;
  color: rgba(29, 34, 40, 0.46);
  font-size: 24rpx;
  line-height: 1.45;
}

.outfit-select {
  max-height: 70vh;
}

.search-box {
  display: flex;
  align-items: center;
  height: 88rpx;
  margin-bottom: 20rpx;
  padding: 0 24rpx;
  border: 1rpx solid var(--color-line);
  border-radius: 44rpx;
  background: rgba(255, 255, 252, 0.78);
  box-sizing: border-box;
}

.search-icon {
  margin-right: 12rpx;
  color: rgba(29, 34, 40, 0.34);
  font-size: 34rpx;
}

.search-input {
  min-width: 0;
  flex: 1;
  height: 84rpx;
  color: var(--color-ink);
  font-size: 30rpx;
  line-height: 84rpx;
}

.outfit-select__list {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
  max-height: 56vh;
  overflow-y: auto;
}

.outfit-option {
  display: flex;
  align-items: center;
  gap: 18rpx;
  padding: 18rpx;
  border: 1rpx solid var(--color-line);
  border-radius: 28rpx;
  background: rgba(255, 255, 252, 0.84);
}

.outfit-option__cover {
  display: grid;
  width: 96rpx;
  height: 96rpx;
  flex: 0 0 96rpx;
  overflow: hidden;
  border-radius: 22rpx;
  background: var(--color-mist);
}

.outfit-option__cover--empty {
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(29, 34, 40, 0.32);
  font-size: var(--font-body);
  font-weight: 800;
}

.outfit-option__main {
  min-width: 0;
  flex: 1;
}

.outfit-option__name {
  overflow: hidden;
  color: var(--color-ink);
  font-size: var(--font-body);
  font-weight: 800;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.outfit-option__meta {
  overflow: hidden;
  margin-top: 8rpx;
  color: var(--color-muted);
  font-size: var(--font-caption);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.sheet-empty {
  padding: 40rpx 0 20rpx;
  text-align: center;
}

.sheet-empty__title {
  color: var(--color-muted);
  font-size: var(--font-body);
}

.sheet-empty__copy {
  margin-top: 8rpx;
  color: rgba(29, 34, 40, 0.42);
  font-size: var(--font-caption);
  line-height: 1.5;
}
