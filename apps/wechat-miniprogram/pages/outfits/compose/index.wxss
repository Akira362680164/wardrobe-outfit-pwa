.page-shell { padding: calc(env(safe-area-inset-top) + 152rpx) 28rpx 220rpx; }

.form-card,
.status-card,
.select-card {
  box-sizing: border-box;
  border: 1rpx solid var(--color-line);
  background: rgba(255, 255, 252, 0.78);
  box-shadow: var(--shadow-card);
}

.form-card {
  margin-top: 28rpx;
  padding: 28rpx;
  border-radius: var(--radius-card-inner);
}

.field-label {
  margin-bottom: 14rpx;
  color: var(--color-ink);
  font-size: var(--font-caption);
  font-weight: 700;
}

.text-input {
  height: 88rpx;
  padding: 0 24rpx;
  border-radius: var(--radius-control);
  background: rgba(29, 34, 40, 0.05);
  color: var(--color-ink);
  font-size: var(--font-body);
}

.ai-button {
  box-sizing: border-box;
  width: 100%;
  height: 82rpx;
  margin: 22rpx 0 0;
  border: 0;
  border-radius: var(--radius-control);
  background: var(--color-ai);
  color: var(--color-surface);
  font-size: var(--font-body);
  font-weight: 700;
  line-height: 82rpx;
}

.ai-button::after { border: 0; }

.ai-result {
  margin-top: 18rpx;
  color: var(--color-muted);
  font-size: var(--font-caption);
  line-height: 1.55;
}

.status-card {
  margin-top: 28rpx;
  padding: 32rpx;
  border-radius: var(--radius-card-inner);
  color: var(--color-muted);
  font-size: var(--font-body);
}

.status-title,
.section-title {
  color: var(--color-ink);
  font-size: var(--font-section);
  font-weight: 700;
}

.status-copy {
  margin-top: 12rpx;
  color: var(--color-muted);
  font-size: var(--font-body);
  line-height: 1.55;
}

.mini-button,
.save-button {
  box-sizing: border-box;
  height: 88rpx;
  border: 0;
  border-radius: var(--radius-control);
  font-size: var(--font-body);
  font-weight: 700;
  line-height: 88rpx;
}

.mini-button {
  width: 240rpx;
  margin-top: 28rpx;
  background: var(--color-primary);
  color: var(--color-surface);
}

.mini-button::after,
.save-button::after { border: 0; }

.garment-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-top: 32rpx;
}

.section-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
}

.section-count {
  color: var(--color-muted);
  font-size: var(--font-caption);
}

.select-card {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 18rpx;
  border-radius: var(--radius-control);
}

.select-card--active {
  border-color: rgba(53, 92, 125, 0.38);
  background: rgba(53, 92, 125, 0.08);
}

.thumb {
  width: 112rpx;
  height: 112rpx;
  border-radius: 24rpx;
  background: var(--color-mist);
}

.fallback {
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-muted);
  font-size: var(--font-label);
}

.garment-main { min-width: 0; flex: 1; }

.garment-name,
.garment-meta {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.garment-name {
  color: var(--color-ink);
  font-size: var(--font-body);
  font-weight: 700;
}

.garment-meta {
  margin-top: 8rpx;
  color: var(--color-muted);
  font-size: var(--font-caption);
}

.check-dot {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48rpx;
  height: 48rpx;
  border-radius: var(--radius-control);
  border: 2rpx solid rgba(29, 34, 40, 0.18);
  background: rgba(255, 255, 252, 0.72);
  color: transparent;
  font-size: 28rpx;
  font-weight: 800;
}

.check-dot.is-active {
  border-color: rgba(53, 92, 125, 0.36);
  background: rgba(53, 92, 125, 0.12);
  color: var(--color-primary);
}

.bottom-bar {
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  padding: 20rpx 28rpx calc(env(safe-area-inset-bottom) + 24rpx);
  background: rgba(251, 251, 248, 0.86);
  backdrop-filter: blur(28rpx);
}

.save-button {
  width: 100%;
  background: var(--color-primary);
  color: var(--color-surface);
}

.save-button[disabled] {
  background: rgba(29, 34, 40, 0.14);
  color: rgba(29, 34, 40, 0.42);
}
.compose-page{padding-bottom:220rpx}.progress{height:10rpx;margin:20rpx 0;border-radius:999rpx;background:var(--color-mist);overflow:hidden}.progress view{height:100%;background:var(--color-primary)}.cover{width:100%;height:520rpx;margin:18rpx 0;border-radius:22rpx;background:var(--color-mist)}
